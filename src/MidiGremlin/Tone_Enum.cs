namespace MidiGremlin
{
    ///<summary>
    ///Tone is an enumeration which represents the tones with their respectable names. 
    ///</summary> 
    ///
    public enum Tone { A, ASharp, B, C, CSharp, D, DSharp, E, F, FSharp, G, GSharp }
}